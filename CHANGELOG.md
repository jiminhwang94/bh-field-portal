# 변경 이력 (CHANGELOG)

상세한 배경·버그 기록은 [docs/WORKLOG.md](docs/WORKLOG.md) 참고.

## [미배포] - 차량 재고 구글 시트 관리

### 추가
- **차량 재고를 구글 시트로 관리** (시트가 연결돼 있으면 자동, 별도 설정 없음):
  스프레드시트의 **차량재고** 탭이 팀 공유 원본이 된다.
  - 수량 `[-]/[+]` → 시트의 해당 칸에 **즉시 기록** (사무실 서버 불필요, LTE 가능)
  - 차량·품목 추가/삭제 → 탭 전체를 다시 써서 반영
  - **시트에서 직접 고친 내용**(차량 이름·품목·수량·최소보유)도 재고 화면을 열 때 앱에 반영
  - 탭이 없으면 재고 화면을 처음 열 때 기기 재고로 자동 생성
- Apps Script(`google-apps-script.gs`)에 재고 동기화 엔드포인트 추가
  (`inventory: pull / push / qty`) — 기존 리포트 기록과 같은 웹 앱 URL 사용, 재배포 필요
- 재고 화면: `📊 시트 연동` 배지 + `🔄 시트에서 받기` 버튼
- **가이드 3종 시트 열람용 탭**: 가이드를 저장/삭제하면 스프레드시트의
  `오류 코드 가이드 · 하드웨어 교체 SOP · SW·명령어` 탭이 자동 갱신된다
  (앱 → 시트 단방향, 시트에서 고친 내용은 앱에 반영되지 않음)

### 변경
- 시트 재고 관리 중에는 [⬆️ 업데이트]/자동 받기가 차량·재고·수량을 덮지 않는다
  (가이드·항목 설정은 기존대로 서버 동기화)
- **품목 목록은 모든 차량 공용** (수량만 차량별): 품목을 추가하면 모든 차량에
  등록되고(다른 차량은 0), 삭제·이름변경·최소보유 변경도 전 차량에 함께 적용.
  시트의 빈 수량 칸은 0 으로 처리 — 한 차량에서 추가한 품목이 다른 차량 탭에
  안 보이던 문제 해결

## [3.0.0] - 2026-08-17
> **오프라인 앱** — 자료를 기기로 옮겨 인터넷 없이 전부 동작하게 하고, APK 로 설치한다. (D-6=A)

### 추가
- **오프라인 전면 지원**: 가이드 열람·검색·추가·수정·삭제, 리포트 작성(사진 포함),
  재고 수정까지 **인터넷 없이 전부** 동작. 자료를 기기 안 IndexedDB 에 보관
- **대기열(outbox)**: 오프라인에서 누른 시트 업로드·재고 변경을 쌓아 두었다가
  연결되면 **자동 처리** (누를 버튼 없음)
- **APK 안드로이드 앱**: `android/` WebView 껍데기 + GitHub Actions 자동 빌드 →
  Releases 에서 태블릿으로 다운로드·설치 (작업 PC 에 개발 도구 불필요)
- **시트 직접 전송**: 기기 → Apps Script 로 바로 보내 **사무실 밖 LTE 에서도 업로드**
  (브라우저가 막으면 서버 우회 통로로 자동 전환)
- 서비스워커: 앱 화면을 기기에 담아 오프라인에서도 앱이 열림 + 사진을 기기 저장본으로 제공
- 연결 상태 띠(📴 오프라인 / ⏳ 대기 N건), 재고 '반영 대기' 표시, 리포트 `업로드 대기` 상태
- 팀 공통 설정(시트 주소)을 기기로 함께 내려받아 오프라인에서도 전송 가능
- 오프라인 PIN 잠금 해제 (마지막 성공한 PIN 확인값을 기기에 보관)

### 변경
- **서버 역할 축소**: 팀 공개본 보관 + 동기화만 담당.
  기기별 작업본(`data/drafts/`) 구조 제거 — **기기 자체가 작업본**
- 가이드·재고·리포트 CRUD API 제거 (전부 기기에서 처리)
- 리포트는 서버에 저장하지 않음 (기기 → 구글 시트)
- APK 앱을 위해 그 출처에 한해 교차 출처 허용 + `X-Access-Token` 헤더 인증
- 테스트 재구성 (6종): 동기화 모델 · 시트 전송 · APK 연결 규칙 · 오프라인 실행 보장 등

### 제거
- 최근 본 가이드 20건 임시 보관(v2.1 D-4) — 전체 자료가 기기에 있어 역할 종료

### 수정
- 서비스워커 캐시 목록에 `net.js` 누락 → 오프라인에서 빈 화면 (테스트로 고정)
- 사전 확인(preflight) 응답에 허용 출처 헤더가 두 번 나가 브라우저가 거부하던 문제
- 재고 '대기 중' 표시가 전송 완료 후에도 남던 문제 → 대기열을 유일한 근거로 변경
- 재고 수량 변경 후 상단 요약(총 개수)이 갱신되지 않던 문제

### 자료 이전
- v2 에서 서버에 남아 있던 리포트를 앱 첫 실행 시 기기로 자동 이전 (`/api/sync/legacy`)
- 개발 중 실수로 서버 `report` 표를 지웠다가 백업에서 복구 (WORKLOG 참고).
  이후 마이그레이션이 예전 리포트를 지우지 않도록 수정

## [2.1.0] - 2026-08-04
### 추가
- **접근 보호**: 팀 공용 PIN(선택). 기기당 1회 입력, 사진까지 보호, `--reset-pin` 복구
- **재고 수량 즉시 공유**: 수량은 [업데이트] 없이 바로 반영 (품목·차량 추가/삭제는 기존대로)
- **오프라인 완충**: 최근 본 가이드 20건 기기 보관 → 연결 끊겨도 열람·검색
- **하단 탭바**: 홈·재고·새 리포트·이력·설정 (라벨 상시 표시), 항목 설정은 설정 안으로
- 첫 실행 시 [업데이트] 개념 1회 안내
- `tools/backup.py`(자동 백업) · `tools/cleanup_media.py`(미참조 사진 정리)
- GitHub Actions CI — push 마다 테스트 6종 자동 실행
- 설정 화면 아코디언
### 수정
- **동시 [업데이트] 잠금 추가** — 두 사람이 같이 눌러도 데이터가 꼬이지 않음
- **API 응답 `Cache-Control: no-store`** — 브라우저가 오래된 데이터를 재사용하던 문제
- 사진 EXIF 회전 반영 (구형 태블릿에서 눕는 문제)
- CORS `*` 헤더 제거

## [2.0.1] - 2026-08-04
### 수정
- 리포트 사진을 구글 시트에 **이미지로 직접 삽입** (기존: 열리지 않는 링크)
- 업로드 전 사진 자동 축소 (최대 1600px / JPEG, 약 87% 용량 감소)
- 하드코딩 도메인 제거 → 접속한 서버 주소 자동 감지

## [2.0.0] - 2026-08-03
### 변경 (호환성 깨짐)
- 노션 연동 **전부 제거** → 구글 스프레드시트(Apps Script 웹 앱) 업로드로 교체
- 월별 시트 자동 생성: 1행 공백 / 2행 항목명 / 3행부터 데이터
- 실시간 공유 제거 → **공개본/작업본 모델**: [업데이트] 버튼을 눌러야 전체 적용,
  타인 업데이트는 자동 수신 (가져오기 버튼 삭제)
- 오프라인 기능(서비스 워커·대기열·HTTPS 설치 흐름) 전부 제거 — 온라인 전용
- "필드" → "항목 설정", "내보내기" → "업데이트" 명칭 변경
### 추가
- 기기별 작업본(`data/drafts/`), 기기 이름 1회 등록, 충돌 경고 + [내 변경 버리고 최신 받기]
- 이전 버전 DB 자동 마이그레이션

## [1.2.0] - 2026-08-02
### 추가
- 팀 공유: 내보내기/가져오기 + 리비전 + diff 미리보기 + 노란 버튼 알림
- 오프라인: 서비스 워커 캐시, 오프라인 검색, 미전송 대기열, `--https` 모드
- 리포트 기기 공유(카카오톡 등), 앱 설치 버튼

## [1.1.0] - 2026-08-01
### 추가
- 차량 추가/삭제 관리 (vehicle 테이블 분리)
- 노션 DB 스키마 임포트로 리포트 항목 자동 생성 (v2.0 에서 제거)
- PWA(manifest·아이콘), 빌드 해시 기반 화면 코드 자동 갱신
### 수정
- macOS python.org 빌드의 SSL 루트 인증서 부재 대응 (CA 번들 자동 탐색)
- 토큰 칸 URL 입력 등 사용자 실수 검증

## [1.0.0] - 2026-08-01
### 최초 릴리스
- 오류코드/HW SOP/SW 명령어 가이드 CRUD + 통합 검색 + 단계 체크리스트
- 스타리아 차량 재고 `[-]/[+]` 관리
- 동적 리포트 폼 + 자동 임시보관 + 사진 촬영/첨부
- Python 표준 라이브러리 단일 서버 (외부 의존성 0)
