# 변경 이력 (CHANGELOG)

상세한 배경·버그 기록은 [docs/WORKLOG.md](docs/WORKLOG.md) 참고.

## [미배포] - 차량 재고 구글 시트 관리

### 추가
- **차량 재고를 구글 시트로 관리** (시트가 연결돼 있으면 자동, 별도 설정 없음):
  스프레드시트의 **차량재고** 탭이 팀 공유 원본이 된다.
  - 수량 `[-]/[+]` → 시트의 해당 칸에 **즉시 기록** (사무실 서버 불필요, LTE 가능)
  - 차량·품목 추가/삭제 → 탭 전체를 다시 써서 반영
  - **시트에서 직접 고친 내용**(차량 이름·품목·수량·최소보유)도 재고 화면을 열 때 앱에 반영
  - 탭이 없으면 재고 화면을 처음 열 때 기기 재고로 자동 생성
- Apps Script(`google-apps-script.gs`)에 재고 동기화 엔드포인트 추가
  (`inventory: pull / push / qty`) — 기존 리포트 기록과 같은 웹 앱 URL 사용, 재배포 필요
- 재고 화면: `📊 시트 연동` 배지 + `🔄 시트에서 받기` 버튼
- **가이드 3종 시트 열람용 탭**: 가이드를 저장/삭제하면 스프레드시트의
  `오류 코드 가이드 · 하드웨어 교체 SOP · SW·명령어` 탭이 자동 갱신된다
  (앱 → 시트 단방향, 시트에서 고친 내용은 앱에 반영되지 않음)

### 변경
- 시트 재고 관리 중에는 [⬆️ 업데이트]/자동 받기가 차량·재고·수량을 덮지 않는다
  (가이드·항목 설정은 기존대로 서버 동기화)
- **품목 목록은 모든 차량 공용** (수량만 차량별): 품목을 추가하면 모든 차량에
  등록되고(다른 차량은 0), 삭제·이름변경·최소보유 변경도 전 차량에 함께 적용.
  시트의 빈 수량 칸은 0 으로 처리 — 한 차량에서 추가한 품목이 다른 차량 탭에
  안 보이던 문제 해결

## [3.13.0] - 2026-09-04
> **첨부 사진이 모든 사람 화면에 보인다**

### 고침 — 리포트·가이드 사진이 액박으로 뜨던 문제 (1번)
현장에서 리포트를 올리니 사진이 드라이브에는 들어갔는데 앱에서는 전부
액박이 떴다. 원인이 두 가지 겹쳐 있었다.

1. **공유 드라이브는 조직 정책으로 '링크가 있는 누구나' 를 막는다.**
   그런데 예전 코드는 `setSharing` 실패를 `catch {}` 로 조용히 삼켰다.
   그래서 사진이 비공개로 남았는데도 아무 표시가 없었다.
2. **`drive.google.com/thumbnail` 은 파일이 공개일 때만 그림을 준다.**
   구글 로그인 쿠키는 다른 사이트의 `<img>` 요청에 붙지 않기 때문이다
   (SameSite). 그래서 **올린 본인 화면에서도** 안 보였다.

- 공개 설정을 **제대로** 시도한다: 고급 드라이브 서비스(`Drive.Permissions`,
  `supportsAllDrives`) → 실패하면 `DriveApp.setSharing` → 그래도 안 되면
  **실패했다고 알려 준다** (응답의 `mediaPrivate`).
- 공개가 막힌 파일은 **바이트를 직접 받아 그린다**. 새 엔드포인트
  `drive: 'bytes'` 가 Apps Script(시트 주인 권한)로 파일을 읽어 base64 로
  보낸다. 목록은 드라이브 썸네일, 크게 보기는 원본을 쓴다.
  한 번 받은 사진은 기기에 담아 두므로 **두 번째부터는 즉시 뜨고
  오프라인에서도 보인다** (사진 2장 · 시트 호출 1회 → 다시 열면 0회).
- 리포트 이력의 목록 타일·크게 보기, 가이드 단계 사진 모두 같은 방법으로 되살린다.
- 설정에 **[사진 공개 복구]** 버튼을 넣었다. 예전에 올려 비공개로 남은
  사진을 드라이브를 훑어 다시 공개로 바꾼다.

### 고침 — 시트의 첨부 칸이 검은 글씨였던 문제 (1번)
- 주소를 글자로만 넣어서 시트에서 검은색으로 보이고 눌러도 열리지 않았다
  (여러 줄이면 자동 링크도 안 걸린다). 이제 서식 있는 값으로 넣어
  **줄마다 파란 링크**가 된다. 이번 버전 이후에 올리는 리포트부터 적용된다.

### 고침 — 검사 도구
- 가짜 드라이브가 `setSharing` 을 **항상 성공**으로 처리해서, 실제 현장에서
  실패하는 이 버그를 검사가 잡지 못했다. 이제 공유 드라이브 정책을 흉내내
  실패시킬 수 있고, `getId` · `getFolders` · `getFiles` · `getThumbnail` ·
  `getBlob` · `getSize` · `setRichTextValue` 를 갖췄다 (실제 API 에 있는 것들).
- 검사 20개를 더했다: 공개 실패를 감추지 않는지, 비공개 사진도 바이트로
  내려받는지, 첨부 칸이 눌러서 열 수 있는 링크인지, 복구가 정직하게 세는지.

## [3.12.0] - 2026-09-04
> **태블릿에서 잘 보이도록 색을 한 단계 진하게**

### 변경 — 화면 색 (사장님 요청)
- 태블릿을 밝은 현장에서 쓰면 옅은 회색 면이 흰 바탕과 구분되지 않았다
  ("가이드 종류·처리 현황 회색, 재고 보충 필요 빨간 테두리가 티가 안 난다").
  화면 시안에서 알맞아 보이던 색이 실제 기기에서는 너무 옅었다.
- 색 토큰 19개를 한 단계씩 진하게 잡았다. 개별 화면 코드는 손대지 않았다.
  - 면: `--color-surface` `#f7f8fa` → `#eef1f5`, `--color-surface-2` `#f4f6f9` → `#e4e9f0`
  - 경계: `--color-border` `#e6e8ee` → `#d2d8e1`,
    `--color-border-strong` `#dfe3ea` → `#b8c0cc`, `--color-hairline` → `#dde2ea`
  - 보조 글자: `--color-muted` `#6b7280` → `#5a6270`, `--color-faint` → `#6f7784`,
    `--color-disabled` → `#9aa2ae`
  - 부족 표시: `--color-danger-tint` → `#fbe9e7`, `--color-danger-tint-2` → `#f5cfca`,
    `--color-danger-border` `#f0d9d6` → `#db958e` (재고 부족 행 테두리가 실제로 보인다)
  - 상태 면: `--color-ok-tint` → `#d9efe4`, `--color-warn-tint` → `#fdeed8`
  - 옅은 코발트 면: `--color-accent-tint` `#eef3ff` → `#dfe8fc`,
    `--color-accent-tint-2` → `#c3d4f7` (홈 **처리 현황** 타일)
  - 그림자 3종을 약간 진하게 — 카드가 바탕에서 떠 보인다
- **코발트(`#0047ab`)와 본문 글자색은 그대로다** — 잘 보인다고 하셔서 건드리지 않았다.

### 고침 — 검사 도구
- `tests/test_js_parses.mjs` 를 `--experimental-vm-modules` 없이 실행하면
  21개 파일이 전부 "실패" 로 나와 **진짜 깨진 파일과 구분이 안 됐다.**
  플래그가 없으면 스스로 다시 실행하도록 고쳤다.

## [3.11.0] - 2026-09-03
> **가이드 사진을 팀이 함께 본다 · 한 달은 시트 탭 하나**

### 변경 — 가이드 사진 (1·2번)
가이드 단계 사진이 **찍은 기기에만** 있었다. 주소가 `/media/<파일명>` 이라
다른 사람 기기에는 그 파일이 없어 아무것도 안 보였다.

- 리포트 첨부와 **같은 공유 드라이브**에 저장한다. 경로는
  `가이드 / <분류> / <제목> / 사진 /`
  (리포트는 `<매장> / <날짜> / 사진` — 같은 뿌리, 다른 가지)
- 사진을 붙이면 곧바로 올린다. **제목이 아직 없으면**(새 가이드) 폴더 이름을
  정할 수 없으니 **저장 시점에 한 번 더 챙긴다**
- 가이드 시트에 `단계 사진` 열이 생겼다. `1. <주소>` 꼴로 단계 번호와 함께 적어
  몇 번째 단계의 사진인지 잃지 않는다
- 기기 안에만 있는 사진은 시트에 적지 않는다 — 남이 열 수 없는 주소다
- 화면에서는 **리포트 이력처럼** 드라이브 썸네일로 보여 준다.
  드라이브 '보기' 주소를 `<img src>` 에 그대로 넣으면 그림이 아니라 페이지가 와서
  액박이 된다 — 주소에서 파일 id 를 뽑아 미리보기 주소로 바꾼다

### 변경 — 한 달은 탭 하나 (3번)
항목이 달라지면 `2026-09 (2)` 처럼 탭을 갈랐다. 한 달을 여러 탭에서 찾아야 했다.

- **탭은 그 달 하나.** 항목이 달라지면 같은 탭 **맨 아래에 새 항목 줄**을 한 줄
  넣고 그 아래로 쌓는다. 옛 줄은 옛 항목 줄 아래 그대로 남아 어긋나지 않는다
- 항목 줄은 **첫 칸이 `작성일시`** 인 것으로 알아본다 (자료 줄의 첫 칸은 실제
  시각이라 헷갈릴 일이 없다)
- 읽을 때 **줄마다 자기 항목**을 함께 준다. 맨 아래 항목만 믿으면 항목이 바뀌기
  전의 줄이 통째로 어긋나 보인다
- 상태 변경도 **그 줄이 속한 묶음**에서 칸을 찾는다. 2행만 보면 항목이 바뀐 뒤의
  줄에서 엉뚱한 칸에 상태가 적힌다
- 수정할 때 항목 줄을 고쳐 쓰지 않고, 그 묶음의 순서에 맞춰 **이름으로** 값을 놓는다
- 예전 판이 만든 `(2)` 탭은 목록에 계속 보여 준다 — 이미 쌓인 리포트를 못 보게
  하면 안 된다. 새로 만들지만 않는다

### 수정
- 가이드 비교(`same`)가 사진 주소를 보지 않아, 시트에 새 사진이 올라와도
  "같다" 고 넘어가 **다른 사람 사진이 영영 안 내려왔다**
- 하네스의 가짜 드라이브가 주소에 공백을 섞어, 주소를 공백으로 자르는 코드가
  그냥 통과했다. 진짜 드라이브처럼 만들고 파싱도 줄 나머지 전체로 고쳤다

### 검증
- 하네스(실제 `.gs` 실행) 33항목: 같은 탭에 항목 줄 두 개, 줄마다 자기 항목,
  상태가 그 줄 기준 칸에, 갈라진 탭 없음, 가이드 사진 경로
  `/가이드/오류 코드 가이드/E-104 …/사진`, 사진 주소 시트 왕복
- 브라우저: 저장 시 `media` 요청 → 주소가 드라이브로 교체 → 시트 push,
  상세 화면에서 `drive.google.com/thumbnail?id=…` 로 렌더(액박 아님),
  항목 묶음 둘인 시트를 읽어 옛/새 줄의 상태·조치 내용 모두 정상
- 테스트 8종 전부 통과

> ⚠️ **Apps Script 를 다시 배포**해야 이번 세 가지가 동작합니다.
> 가이드 시트에 `단계 사진` 열이 하나 늘어납니다 (기존 내용은 그대로).

## [3.10.0] - 2026-09-03
> **디자인 1c 확정판 전체 적용 (app.css · index.html 교체)**

받은 자료가 "덧붙이는 CSS 조각" 에서 **완성된 교체 파일**로 바뀌어, 지시서 3단계를
그대로 수행했다.

### 변경
- `web/css/app.css` **통째로 교체** (966줄 → 8화면 전체 커버, 다크 모드 없음)
- `web/index.html` **통째로 교체** — 레일이 DOM 첫 자리로, 다크 `theme-color` 제거,
  `color-scheme: light`. `class="tab"`·`data-tab`·`href` 는 유지되어 `app.js` 는 무변경
- `patches/js-changes.md` 적용 — 재고 부족 행 `is-low-row` + `보충 필요` 알약,
  알약 필터 `filter-toggle`, 차량 추가 탭 `veh-tab--manage`, 임시보관 패널 `panel--warn`
- **이모지 전면 제거** (1c 브랜드 톤). 화면에 보이는 이모지 0개 —
  가이드 분류·버튼·토스트·배너·첫 실행 안내·오프라인 띠까지
- `sw.js` 캐시 v3.10.0 (새 CSS 즉시 반영)

### 지킨 것 · 보탠 것
- **[새로고침] 이름 유지.** 디자인 파일에는 `⬆ 업데이트` 로 적혀 있지만, 올리기가
  자동이 된 v3.9 결정에 따라 버튼은 받기 하나만 한다. 모양은 `.btn-update` 그대로
- 교체 CSS 에 없던 `.topbar__spacer`(새 index.html 자신이 쓴다) · `.recent__list` 보완
- 부족 행 hover 색 `#fbf1ef` 을 토큰으로 올림 (디자인 자체 규칙: 토큰 밖 새 색 금지)
- **폰 카드에 이름표 추가** — 표 머리가 숨는 ≤720px 에서 숫자만 둘 남아 무엇이
  보유·최소보유인지 알 수 없었다. `data-label` + `::before` 로 붙였다 (표시만)

### 수정
- 색 계약 검사가 절 제목 문자열로 토큰 경계를 찾다 새 CSS 에서 터졌다 —
  `:root { }` 블록 구조로 찾도록 바꿨다

### 검증
- 디자인의 `preview/all-screens.html` 을 앱의 실제 CSS 로 렌더해 대조
- 스펙 수치 실측: 레일 104 · 항목 76 · 상단바 66 · 검색 76 · 검색버튼 140 · 코드 62,
  칩·버튼 nowrap, 본문 흰 배경 — 전부 일치
- 6화면 (1024×768 · 1440 · 600) 확인. 활성 알약이 화면 따라 이동, 설정은 맨 아래,
  폰 폭에서 하단 탭바 + 재고 카드 전환
- 테스트 8종 전부 통과

## [3.9.0] - 2026-09-03
> **올리기는 자동 · [새로고침] 은 받기 · 좌측 레일 디자인 · 사무실 서버 완전 삭제**

### 바뀐 생각
[⬆ 업데이트] 는 뜻이 두 개였다 — 올리고, 받는다. 그런데 올리기는 인터넷이 되면
이미 자동으로 되고 있어서, 눌러 봐야 하는 일이 없는 것처럼 느껴졌다.
"여러 사람이 써도 섞이지 않게" 가 버튼의 이유였는데, 탭을 통째로 쓰는 자료
(가이드·항목·차량구성)는 **미뤄서 한 번에 올릴수록 더 많이 덮어쓴다.**
섞임을 막는 방법은 버튼이 아니라 **올리기 전에 받아서 합치는 것**이다.

### 변경
- **올리기는 자동만.** 버튼은 **[새로고침]** 하나 — 다른 사람 변경을 지금 받아온다.
  앱으로 돌아올 때와 5분마다 자동으로도 받아온다
- **올리기 전에 시트 것과 합친다.** 가이드·항목·차량/품목은 내가 바꾼 줄만 얹고
  나머지는 시트 것을 그대로 둔다. A 와 B 가 각자 다른 줄을 고쳐 올려도 둘 다 남는다
- **상단 칩은 밀린 것이 있을 때만 숫자를 보인다** — 오프라인(주황) · 올리기 실패(빨강).
  정상일 때 숫자가 보이면 무언가 잘못됐다는 뜻이 되어 신호가 분명하다
- **[올릴 내용] 되돌리기가 "바꾸기 전으로"** 정확히 돌아간다. 대기열이 변경마다
  **바꾸기 전 모습**(changes)을 함께 담는다. 오프라인에서 **새로 만든** 리포트·가이드·
  품목을 취소하면 그 자료도 지운다 (올라간 적이 없으니 남길 이유가 없다)
- **디자인 1c "좌측 레일"** 적용 — 하단 탭바 → 104px 좌측 레일(설정은 맨 아래),
  다크 모드 완전 제거(태블릿이 검게 되던 원인), 둥근 흰 카드 행, 재고 부족 행은 빨강 톤
- **사무실 서버 완전 삭제.** `app/` 패키지, 동기화 API, 우회 경로, 서버 전용 테스트 5종.
  `server.py` 는 화면 파일만 내준다. APK 는 서버 없이 동작한다

### 수정
- **이력 → 리포트 상세의 사진 액박** (APK). 기기 안 파일에서 꺼내도록 고쳤다.
  영상과 [열어 보기] 링크도 함께
- 되돌리기를 한 번 하면 목록이 사라지던 것 — 라우터가 창을 닫아 버려서였다.
  시트가 닫힐 때만 뒤 화면을 다시 그린다
- "올리지 못한 것 0건" 이라는 말이 안 되는 칩 — 대기열이 비면 실패 표시도 지운다
- 재고 화면 안의 잘못된 `.screen` 중첩 제거

### 검증
- 브라우저(1024×768 · 1440): 6화면 디자인 확인, 재고 부족 행·필터 알약·레일 활성 표시
- 오프라인에서 가이드·항목·품목을 새로 만든 뒤 7건 연속 되돌리기 → 전부 삭제, 대기열 0
- 가이드 요약 수정 + 품목 삭제 → 되돌리기 → 원문·수량(6) 그대로 복구
- 테스트 8종 (`test_static_host.py` 새로 추가) 전부 통과

> ⚠️ 이번 판부터 태블릿은 사무실 서버가 필요 없습니다. `tools/backup.py` 자동 백업은
> 더 이상 백업할 서버 자료가 없어 비어 있는 폴더를 만듭니다 — `백업 자동실행 해제.bat` 으로
> 끄셔도 됩니다 (자료는 구글 시트·드라이브에 있습니다).

## [3.8.0] - 2026-09-03
> **사무실 서버 완전 제거 · 리포트 항목을 팀 공통으로**

### 수정
- **연결 테스트가 계속 "서버 주소가 설정되지 않았습니다" 라고 하던 문제.**
  구글로 곧바로 보내다 실패하면 **사무실 서버로 우회**하게 되어 있었다.
  우회가 먼저 "서버 주소가 없다" 며 멈추는 바람에, **진짜 원인이 무엇이든
  늘 서버 얘기만** 나왔다.
  - 우회를 없앴다. 이제 막힌 이유를 그대로 알려 준다
    (인터넷 · `/exec` 로 끝나는지 · 액세스 권한 '모든 사용자')

### 변경
- **앱에서 사무실 서버를 통째로 걷어냈다.** 팀이 함께 보는 원본은
  **구글 시트 하나**뿐인데, 서버로 보내는 통로가 남아 있어 태블릿에서
  엉뚱한 알림만 냈다. 지운 것 —
  `request()` · `serverRequest` · `state()` · `publish` · `takeLatest` ·
  `pull` · `push` · 서버 경유 재고 반영 · 서버 못 닿음 표시 · 버전 문의

- **리포트 항목이 팀 공통이 되었다.** 한 사람이 항목을 바꾸고 [⬆ 업데이트]
  하면, 시트의 **[리포트 항목] 탭**을 거쳐 **모든 기기가 같은 항목**을 쓴다.
  사람마다 항목이 다르면 같은 달 시트가 사람 수만큼 갈라지기 때문이다.
  - 항목을 고치면 시트 반영이 예약된다 (여러 번 고쳐도 한 건)
  - 안 올린 내 변경이 있으면 **받아오지 않는다** (덮어쓰면 방금 고친 것이 사라진다)
  - 시트가 비어 있으면 **아무것도 하지 않는다** (아무도 안 올린 것이지
    전부 지우라는 뜻이 아니다)
  - 시트에 적힌 **순서가 곧 리포트 시트의 열 순서**다

### 검사
- **`node --check` 를 믿을 수 없다는 것을 확인했다.** 문자열 한복판에 진짜
  줄바꿈이 들어가 파일이 깨졌는데도 조용히 통과했고, 브라우저에서만
  `SyntaxError` 가 나며 화면이 하얘졌다 (이 저장소에서 다섯 번째다).
  - 새 검사 `tests/test_js_parses.mjs` — 파일을 **ES 모듈로 진짜 파싱**한다.
    브라우저와 같은 방식이라 깨진 문자열이 반드시 걸린다
- 하네스의 가짜 Range 에 `clearContent()` 가 없어, 그것을 쓰는 코드가
  조용히 터지고도 검사가 통과했다. 채워 넣었다

### 검증
- 브라우저: 연결 테스트가 서버 얘기 없이 진짜 원인을 말함 → 시트 연결 후 성공
- 브라우저: 항목 수정 → 대기열에 `fieldsheet-push` 1건 (세 번 고쳐도 1건),
  다른 사람이 올린 항목을 받으면 그대로 반영되고 시트에 없는 항목은 빠짐
- 하네스(실제 `.gs`): 항목 올리기·받기·종류·선택지·필수·순서·ID 유지·삭제 반영
- 테스트 12종 전부 통과

> ⚠️ Apps Script 를 **다시 배포**해야 [리포트 항목] 탭이 동작합니다.

## [3.7.0] - 2026-09-03
> **사진 액박 · 리포트 항목 고정 · 사무실 서버 제거 · 올릴 내용 목록 · 아이콘에 X**

### 수정
- **APK 에서 가이드 단계 사진이 액박으로 뜨던 문제.**
  사진은 **기기 안(IndexedDB)** 에 저장되는데, 화면이 가리키는 주소는
  `/media/<파일명>` 이라는 **서버 경로**였다. 브라우저로 접속하면 서버가
  그 경로로 파일을 내주니 우연히 보였지만, APK 는 화면 파일이 기기 안에
  있어 그 경로에 아무것도 없다. 이제 기기 안 파일을 직접 꺼내 보여 준다.
  리포트 첨부 미리보기도 같은 문제였고 함께 고쳤다.

- **사진이 화면을 뒤덮던 문제.** 폭만 막혀 있어 3000×2000 사진이 화면 몇 개
  높이로 늘어났다. **320×200 안**으로 묶고, 누르면 크게 볼 수 있게 했다.

- **APK 를 새로 설치하면 리포트 항목이 하나도 없던 문제.**
  항목을 사무실 서버에서 받아오게 되어 있었는데, 새 기기는 서버 주소를
  모르니 빈 상태로 시작했다. 이제 **기본 항목 9개가 앱에 붙박이로** 들어 있다.

- **급히 만든 항목 하나 때문에 시트의 기존 데이터가 어긋나던 문제.**
  항목이 바뀌면 그 달 탭의 2행 머리를 최신으로 **덮어썼다.** 그 아래 이미
  쌓인 줄들은 옛 항목 순서 그대로인데 머리만 바뀌니 열과 값이 통째로
  어긋났다.
  - 이미 있는 탭의 머리는 **절대 고쳐 쓰지 않는다**
  - 항목이 달라지면 **다음 탭**(`2026-09 (2)`)에 새로 시작한다
  - 옛 항목으로 다시 올리면 **옛 탭으로 되돌아간다**
  - 이력 화면은 갈라진 탭도 같은 달로 본다

### 변경
- **사무실 서버 주소를 없앴다.** 팀이 함께 보는 원본은 구글 시트 하나뿐인데,
  [업데이트] 가 서버에도 따로 올리려다 매번 실패해 **빨간 알림만 띄웠다.**
  실제로 하는 일이 없어서 통째로 뺐다. 이제 인터넷만 되면 어느 와이파이에서도
  된다. 설정의 주소 칸과 빨간 띠도 사라졌다.

- **정체 모를 "올릴 내용 1건" 의 정체.** 가이드를 고치면 대기열에도 들어가고
  `dirty` 표시도 서면서 **같은 변경을 두 번 셌다.** 이제 대기열 하나만 센다.

- **[올릴 내용 N건] 을 누르면 목록이 열린다.** 무엇이 아직 안 올라갔는지
  한 줄씩 보여 준다.
  - 항목을 누르면 **고칠 수 있는 화면으로** 넘어간다
  - [되돌리기] 를 누르면 그 줄 안에서 한 번 더 묻고, **바꾸기 전 값**으로
    되돌린다. 자료를 지우는 것이 아니다
  - [+] 를 여러 번 눌러 쌓인 것도 **맨 처음 값**으로 되돌아간다

- **앱 아이콘의 육각형 안에 회사 X 로고를 넣었다.** 원본대로 왼위→오른아래는
  끊기지 않는 긴 획, 나머지 둘은 그 획에 닿기 전에 멈춘다. X 는 육각형보다
  가늘다 — 같은 굵기면 48dp 런처에서 한 덩어리로 뭉갠다.

### 검증
- 브라우저: 3000×2000 사진이 299×200 으로, `src` 는 blob(기기 안 파일),
  눌러서 785px 로 확대·축소, 단계 체크가 잘못 눌리지 않음
- 하네스(실제 `.gs` 실행): 항목이 바뀌면 옛 탭 머리 그대로 · 줄도 안 늘어남 ·
  새 탭 생성 · 옛 항목은 옛 탭으로 복귀 · 월 목록에 둘 다
- 브라우저: 칩 → 목록 → 줄 안에서 되묻기 → 수량 14 로 복구, 대기열 0
- 검사 `test_design_contract.py` 를 50항목으로, 테스트 11종 전부 통과

> ⚠️ Apps Script 를 **다시 배포**해야 항목 고정·새 탭 동작이 적용됩니다.

## [3.6.1] - 2026-09-03
> **앱 아이콘 · 재고 수량 버튼 · 최소보유 수정**

### 수정
- **재고에서 [+] [−] 가 아예 안 먹히던 문제.** 재고를 표로 바꾸면서 수량 칸의
  `data-qty` 표시를 빠뜨렸다. 누르면 핸들러가 그 칸을 못 찾아 **중간에서 터졌다.**

- **최소보유만 늘렸는데 보유 수량이 바뀌던 문제.** 위와 **같은 원인**이었다.
  버튼을 누르면 화면을 고치기 **전에** 기억 속 수량을 먼저 1 올리는데, 바로
  다음 줄에서 터지니 화면은 그대로인 채 **기억 속 값만 올라간 상태**로 남았다.
  그 상태로 [수정] 창을 열면 그 틀어진 값이 보유 칸에 채워지고, 저장하는
  순간 시트에 그대로 박혔다.

  - 수량 칸에 `data-qty` 를 되돌렸다
  - 칸을 못 찾아도 멈추지 않는다 (`paintQty` 가 조용히 넘어간다)
  - 실패하면 **원래 값으로** 되돌린다 (보낸 만큼 빼는 게 아니라)
  - 수정 창은 **손대지 않은 칸을 보내지 않는다** — 최소보유만 고쳤으면
    보유 수량은 건드리지 않는다

### 변경 — 앱 아이콘
태블릿 홈 화면의 아이콘이 디자인 이전 그대로였다 (남색 바탕 · 빨강/주황/청록 막대).

- **적응형 아이콘이 아예 없었다.** 요즘 런처는 이게 없으면 옛 PNG 를 흰 판에
  욱여넣어 촌스럽게 보인다. 벡터로 제대로 만들었다
- 마크는 **허니컴 한 칸**(정육각형 테두리) — 상단바 브랜드 마크와 같은 언어다.
  코발트 바탕 · 흰 도형, 색은 디자인의 세 가지 안에서만 썼다
- 안드로이드 4종 · 웹 4종(192 · 512 · maskable · apple-touch)을 같은 도형으로 다시 구움
- **앱을 켜는 순간의 색**도 옛 남색에서 흰색으로. 예전에는 켤 때만 남색이
  번쩍이고 곧 흰 화면으로 바뀌어 어색했다

### 검증
- 브라우저: [+] 세 번 → 화면·저장값 모두 9, [−] 한 번 → 8/8
- 브라우저: 실제 [수정] 창에서 최소보유 4→8, **보유 8 그대로 유지**
- 검사 `test_design_contract.py` 를 28항목으로 늘렸다 —
  마크업이 손잡이(`data-*`)를 빠뜨리거나 아이콘이 옛 색으로 돌아가면 잡힌다
- 테스트 11종 전부 통과

## [3.6.0] - 2026-09-02
> **화면을 디자인 모음집 구조로 다시 맞췄다**

### 원인
`app.css` 는 이미 디자인 것이었다. 클래스도 하나 빠짐없이 있었다.
그런데 화면은 전부 예전 모습이었다. 범인은 갈아끼울 때 덧붙인 두 줄이다.

```css
.page-head h1  { flex: 1 1 100%; }   /* 제목이 한 줄을 통째로 */
.page-head > p { flex: 1 1 100%; }   /* 설명이 또 한 줄 */
```

옛 마크업을 살려 두려던 호환 레이어였는데, 이것이 디자인의 **한 줄 머리**
(제목 · 설명 · 오른쪽 버튼)를 세 줄로 쪼갰다. 스타일시트는 새것인데
화면만 옛것이라, CSS 를 아무리 대조해도 원인이 안 보였다.

### 변경 — 화면별
- **재고**: 카드 목록 → **표** (부품 · 보유 · 최소보유 · 수량 조절).
  ISO 시각이 줄마다 노출되던 것도 사라졌다. 아래에 상태 줄을 붙였다
- **새 리포트**: 1열 → **2열 격자**. 여러 줄 항목과 첨부는 두 칸을 다 쓴다.
  버튼은 아래 오른쪽에 모으고 왼쪽에 안내를 둔다
- **가이드 목록 · 항목 설정**: 두 줄 카드 → 디자인의 **한 줄 행**
  (코드 · 제목+요약 · 열기). 배지를 늘어놓는 대신 요약 줄에 숫자를 모았다
- **이력**: 아래에 상태 줄 (변경 즉시 기록 안내 ↔ 마지막 수신 시각)
- **가이드 상세 · 리포트 상세 · 검색**: 되돌아가기 + 한 줄 머리
- **설정**: 한 줄 머리(기기 이름 · 앱 버전), 한 칼럼(820px)으로 폭 제한

### 변경 — 전체
- 화면 머리 6곳을 `page-head__title` 로 통일. 이모지 제목을 뺐다
- 버튼·패널 제목의 그림문자 **37개** 제거.
  디자인이 남긴 `⬆ 업데이트` · `📖 설치 방법` · `＋` · `−` · `↑↓` 만 남겼다
- 화면에 ISO 시각을 그대로 쓰지 않는다. `when()` 이
  `2026-09-02T14:03:50` → `09-02 14:03` 으로 줄인다 (해가 넘어가면 연도를 남김)

### 검증
- 디자인 모음집의 각 화면(1024×768)과 나란히 놓고 눈으로 대조
- 새 검사 `tests/test_report_draft.py`(18항목) · `tests/test_design_contract.py`(24항목)
  — 호환 레이어가 되살아나거나 옛 `<h1>` 이 돌아오면 잡힌다
- 테스트 11종 전부 통과

## [3.5.0] - 2026-09-02
> **[새 리포트] 가 수정 화면으로 넘어가던 문제 · 앱 전체 속도**

### 수정
- **[새 리포트] 를 누르면 "리포트 수정" 화면이 열리고 예전 수정 내용이
  적혀 있던 문제.** 한 번 리포트를 수정하고 나면, 그 뒤로는 새 리포트를
  아예 쓸 수 없는 상태가 됐다.

  임시보관 칸이 **하나뿐**이었다. 거기에 "어느 시트 줄을 고치는 중인지"까지
  같이 담아 두었더니, 새 리포트로 들어와도 그 줄 번호를 그대로 주워 왔다.

  - 임시보관을 **둘로 나눴다.** 새 리포트 칸은 줄 번호를 **절대 갖지 않는다**
  - 무엇을 하는 화면인지는 임시보관이 아니라 **주소** 가 정한다
    (`#/report/new` = 언제나 빈 새 리포트, `#/report/edit` = 수정)
  - 새 리포트에 쓰다 만 내용은 그대로 남고, 수정하던 내용도 따로 남는다
  - 다른 줄을 고치러 들어오면 예전 수정 내용은 버린다
  - 이력을 거치지 않고 수정 주소로 바로 들어오면 이력으로 돌려보낸다

### 속도
- **무엇을 눌러도 느리던 문제.** 앱이 서버에 **초당 50건 넘게** 같은 질문을
  반복하고 있었다.

  요청이 끝날 때마다 "서버 상태가 바뀌었다" 고 무조건 알렸는데, 그 알림을
  받은 화면이 상태를 다시 물어보고, 그 물어보는 것도 요청이라 또 알림이
  가서, 요청들이 서로를 부르며 끝없이 돌았다. 브라우저가 한 서버에 동시에
  여는 연결은 6개뿐이라 **진짜 요청들이 전부 그 뒤에 줄을 섰다.**
  화면 전환도, 저장도, 사진 올리기도 전부 이 줄 뒤에서 기다리고 있었다.

  - 서버 상태는 **값이 실제로 바뀔 때만** 알린다
  - 동시에 들어온 상태 확인은 **한 번만** 물어본다
  - 상태를 읽을 때 기기 저장소를 한 줄씩 기다리지 않고 한꺼번에 훑는다

  가만히 둔 3초 동안: **약 150건 → 0건**

- **시트를 기다렸다가 화면을 그리던 것을 바꿨다.** 재고와 이력은 시트에서
  받아오는 데 몇 초가 걸리는데, 그 동안 빈 화면만 보고 기다려야 했다.
  이제 **기기에 있는 내용을 먼저 그려 주고**, 시트는 뒤에서 받아 조용히
  다시 그린다. 창을 열어 놓고 무언가 적는 중이면 다시 그리지 않는다.

  화면을 여는 데 걸리는 시간(순수 측정): 재고 7ms, 새 리포트 5ms,
  항목설정 3ms, 설정 65ms

### 검증
- 브라우저: 수정 → 새 리포트 진입 시 **빈 화면**, 새 리포트 임시보관 유지,
  수정 재진입 시 그 줄 내용 유지, 수정 주소 직접 진입 시 이력으로 되돌림
- 브라우저: 가만히 둔 3초 동안 서버 요청 0건
- 새 검사 `tests/test_report_draft.py` 18항목 — 두 사고가 다시 나면 잡힌다
- 테스트 10종 전부 통과

## [3.4.1] - 2026-09-02
> **수정할 때 첨부가 사라지던 문제 · 드라이브 경로 단순화**

### 수정
- **이력에서 [수정]으로 들어가면 사진·영상이 사라지던 문제.**
  수정 화면이 기존 첨부를 넘겨받지 않아, 저장하면 시트의 링크가 **빈 칸으로 덮였다.**
  드라이브에는 파일이 남아 있지만 리포트에서는 영영 사라지는, 되돌릴 수 없는 손실이었다.
  - 이제 기존 첨부를 화면에 보여 주고 **그대로 유지**한다
  - 새 첨부를 더하면 **뒤에 붙는다** (기존 것을 덮지 않는다)
  - 빼고 싶으면 ✕ 로 그것만 뺀다
- **수정 중 폼을 벗어났다 돌아오면 새 줄로 저장되던 문제.**
  임시보관에 "어느 줄을 고치는 중인지"가 빠져 있었다. 함께 담도록 했다.
- 썸네일을 못 받아올 때 첨부 칸이 찌그러지던 것 (높이 고정)
- 첨부 개수 배지가 기존 첨부를 세지 않던 것

### 변경
- **드라이브 저장 경로를 단순하게 바꿨다.** 받은 폴더 주소가 이미
  `필드팀 유지보수(이미지,동영상)` 인데 그 안에 같은 이름 폴더를 또 만들고 있었다.

  ```
  전: 필드팀 유지보수(이미지,동영상)/필드팀 유지보수(이미지,동영상)/사진/매장/날짜/
  후: 필드팀 유지보수(이미지,동영상)/매장/날짜/사진/
  ```

  받은 폴더를 **그대로 뿌리로** 쓰고, **매장 → 날짜 → 사진/동영상** 순으로 내려간다.
  매장을 먼저 찾고 그 날 무엇이 있었는지 보는 순서라, 드라이브에서 직접 뒤질 때 빠르다.

### 검증
- 하네스(실제 `.gs` 실행): 새 경로 확인, 같은 이름 폴더를 또 만들지 않음,
  수정 시 기존 첨부 유지 + 새 첨부 덧붙임 + 뺀 것만 빠짐
- 브라우저: 수정 진입 시 기존 첨부 2개 표시 → 저장 후에도 2개 유지,
  새 첨부 추가 시 3개, 하나 빼면 2개, 줄 수는 계속 1개
- 테스트 9종 전부 통과

> ⚠️ 이미 예전 경로에 올라간 첨부는 그 자리에 남는다. 시트의 링크도 그대로 동작한다.
> 새로 올리는 것부터 새 경로로 들어간다.

## [3.4.0] - 2026-09-02
> **첨부를 공유 드라이브로 · 영상 재생 · 이력 버튼 정렬**

### 변경 (호환성 주의)
- **첨부가 팀 공유 드라이브에 저장된다.** 지금까지는 스크립트를 배포한 사람의
  **개인 드라이브**에 들어갔다. 그 사람 계정 용량을 쓰고, 계정이 바뀌면 사진이
  통째로 사라진다. 이제 공유 드라이브의 `필드팀 유지보수(이미지,동영상)` 폴더로 간다.
- **폴더가 종류·매장·날짜로 나뉜다.**

  ```
  필드팀 유지보수(이미지,동영상)/
    사진/   옥동식 서초점/ 2026-09-02/ 현장사진.png
    동영상/ 옥동식 서초점/ 2026-09-02/ 증상영상.mp4
  ```

  폴더 이름에 못 쓰는 글자(`/ : * ? " < > |`)는 걸러낸다.
  같은 매장·같은 날은 폴더를 다시 만들지 않는다.
- 공유 드라이브에 못 닿으면 **업로드를 멈추지 않고** 예전 위치로 저장하되,
  화면에 그 사실을 알린다. 조용히 개인 드라이브로 돌아가면 알 수가 없다.
- 공유 드라이브를 옮기면 `google-apps-script.gs` 의 `SHARED_DRIVE_ID` 만 바꾸면 된다.

### 수정
- **영상이 사진처럼 한 장면만 보이던 문제.** 시트 칸에는 주소만 있어서 사진인지
  영상인지 알 수 없었다. Apps Script 에 `drive: 'info'` 를 두어 종류를 알아 오고,
  영상은 **드라이브 재생기로 띄운다**. 목록에서는 ▶ 표시가 붙는다.
  한 번 알아낸 종류는 기기에 담아 두고 다시 묻지 않는다.
- **이력 상세의 [수정]·[이어서 작성]·[삭제] 가 한 줄에 나란히** 놓인다.
  좁은 화면에서도 갈라지지 않게 세 칸으로 나눴다.

### 추가 (재발 방지)
- `tests/test_apps_script.mjs` 에 **가짜 드라이브**를 넣어 폴더 트리가 실제로
  만들어지는지 확인한다 — 사진/영상이 각자 폴더로 가는지, 매장·날짜 폴더가
  생기는지, 같은 날 두 번째 업로드가 폴더를 새로 만들지 않는지, 못 쓰는 글자를
  거르는지, 영상을 영상으로 알려 주는지.
- `tests/test_css_contract.py` 에 **문자열이 줄 중간에서 끊긴 곳** 검사를 넣었다.
  편집 중 줄바꿈 이스케이프가 진짜 줄바꿈이 되는 사고가 이 저장소에서 네 번 났고,
  `node --check` 는 통과하는데 브라우저에서만 터져 화면이 통째로 비었다.
  일부러 깨뜨려 검출되는 것까지 확인했다.

### 검증
- 브라우저 실동작: 사진+영상 첨부 업로드 → 폴더 경로 확인
  (`필드팀 유지보수(이미지,동영상)/사진/옥동식 서초점/2026-09-02`),
  영상에만 ▶ 표시, 뷰어에서 사진은 이미지·영상은 재생기(iframe),
  버튼 3개가 같은 줄(y=244)
- 테스트 9종 전부 통과

## [3.3.2] - 2026-09-02
> **가이드 동기화 정상화 · 이력 수정/삭제 · 단계 체크 UI**

### 수정
- **가이드가 [업데이트] 누를 때마다 하나씩 늘어나던 문제 (1·2번).**
  `writeGuideSheet` 이 헤더는 7칸(ID 열 포함)인데 데이터는 **6칸만** 썼다.
  Apps Script 는 칸 수가 다르면 예외를 던지는데, 그 직전에 `clearContents()` 가
  이미 실행돼 **탭이 비워진 채로 남았다.** 그래서:
  - 시트에 ID 가 안 적혔고 → 앱이 매번 "새 가이드"로 보고 복사본을 쌓았다 (1·2번)
  - 새 가이드를 만들어도 시트에 반영되지 않았다 (3번)
  - 기존 가이드를 고치면 **그 분류 탭이 통째로 사라졌다** (4번)
  네 가지가 전부 이 한 줄에서 나왔다.
- **이미 늘어난 복사본을 정리한다.** [⬆ 업데이트] 한 번에 같은 분류·같은 제목의
  복사본을 하나로 합친다(가장 먼저 만든 것을 남긴다). 결과는 토스트로 알린다.
- **ID 가 비어 있어도 제목으로 짝을 찾는다.** 예전 시트에는 ID 열이 없어서
  ID 만 믿으면 같은 사고가 다시 난다.
- **[이어서 작성] 후 저장하면 줄이 두 개가 되던 문제 (6번).**
  이력에 **[✏️ 수정]** 을 새로 두었다. 이쪽으로 들어가면 시트의 **그 줄을 고쳐 쓴다**.
  기존 [📝 이어서 작성] 은 그대로 새 방문으로 남는다 — 재방문 기록이 필요하기 때문이다.

### 추가
- **이력에서 리포트 삭제 (7번)** — 상세의 **[🗑 삭제]** 로 구글 시트의 그 줄을 지운다.
  지우면 아래 줄 번호가 당겨지므로 그 달 사본을 비워 다시 받아오게 했다.
  드라이브의 사진·영상은 지우지 않는다(다른 기록이 참조할 수 있어서).
- Apps Script 에 `reports: 'update'` / `reports: 'delete'` 추가

### 변경
- **단계별 절차의 체크 표시를 새로 만들었다 (5번).** 이모지(`○`/`✅`) 대신
  선으로 그린 표시가 코발트로 차오른다. **줄 전체가 버튼**이라 장갑 낀 손으로도
  누르기 쉽다(655×64). 움직임을 끈 기기는 애니메이션 없이 동작한다.

### 추가 (재발 방지)
- `tests/test_apps_script.mjs` — **가짜 스프레드시트로 실제 `.gs` 를 실행한다.**
  이 파일은 구글 서버에서만 도는 코드라 지금까지 "같은 낱말을 쓰는지"만 봤고,
  그 사이 실제 버그가 두 번 통과했다. 이제 가이드 왕복·리포트 기록·수정·삭제를
  진짜로 돌려 확인한다. 칸 수를 되돌려 보면 실패하는 것까지 확인했다. CI 에도 추가

### 검증
- 브라우저 실동작 전부 확인:
  [업데이트] 3회 반복 → 7건 유지·중복 0 / 새 가이드 → 시트 반영 /
  가이드 수정 → 분류 유지·내용 반영 / 단계 체크 SVG·줄 전체 터치 /
  리포트 수정 → 2건 유지·`E-204→E-777` / 삭제 → 1건으로 줄어듦
- 테스트 9종 전부 통과

## [3.3.1] - 2026-09-02
> **재고 화면 UI 깨짐 수정 + 전 화면 점검**

### 수정
- **재고 화면의 버튼이 브라우저 기본 모양으로 찌그러져 있던 문제.**
  v3.2 에서 디자인을 통째로 갈아끼울 때 이 화면이 쓰던 클래스가 정의를 잃었다.
  - 차량 탭(`.tabs`/`.tab`) — `.tab` 은 새 디자인에서 **하단 탭바** 를 뜻하게 되어,
    차량 탭이 탭바 모양(세로 정렬·작은 글자)을 뒤집어썼다.
    디자인이 이미 가진 `.veh-tabs`/`.veh-tab` 으로 바꿨다
  - `[부족 항목만]`·`[시트에서 받기]`(`.chip`) — 정의가 없어 기본 버튼이었다.
    켜고 끄는 필터는 `.filter-toggle`, 나머지는 보조 버튼으로 통일
  - 수량 `[−]`/`[＋]`(`.qty__btn`) — 테두리·배경이 없어 기본 버튼이었다. 52×52 로 맞춤
- **다크 모드에서 여러 줄 입력칸이 흰 배경 + 검은 글자**였다.
  `<textarea class="textarea">` 가 `.input` 스타일을 못 받고 있었다
- 가이드 목록 검색칸에 클래스가 없어 같은 문제가 있었다
- **버튼 최소 높이를 44px 로** — 여러 버튼이 40px 라 디자인 기준에 미달했다
- 항목 설정 모달에서 체크박스가 옆 드롭다운과 높이가 안 맞던 것

### 추가
- `tests/test_css_contract.py` — 화면이 쓰는 클래스가 CSS 에 있는지,
  입력칸에 스타일이 붙어 있는지, 하단 탭바의 `.tab` 을 빌려 쓰지 않는지,
  토큰 밖에서 새 색을 만들지 않는지 검사. **이 종류의 사고는 코드가 멀쩡하고
  테스트도 통과해서 눈으로 보기 전에는 알 수 없다.** CI 에도 추가

### 검증
- 8개 화면 전수 점검: 브라우저 기본 스타일 잔재 0건, 44px 미만 조작 요소 0건,
  가로 넘침 0건 (라이트·다크 · 태블릿 768×1024)
- 수량 연타 시 2개가 정확히 빠지는 것 재확인 (v3.3 의 직렬화 수정)
- 테스트 8종 전부 통과

## [3.3.0] - 2026-09-01
> **후보 11건 반영** — 현장 편의 4 · 사고 예방 4 · 운영 3

### 추가 (현장)
- **같은 식당 지난 방문 자동 표시** — 새 리포트에서 식당명을 넣으면 그 식당의 지난 기록이
  바로 아래 뜬다. 이력 화면으로 건너가지 않아도 된다. 기기에 받아 둔 사본에서 찾으므로
  오프라인에서도 되고 네트워크를 쓰지 않는다
- **[이어서 작성]** — 이력의 한 건에서 눌러 식당명·로봇 시리얼·오류 코드가 채워진
  새 리포트를 연다. 재방문이 잦은 업무라 같은 값을 다시 타이핑하지 않아도 된다
- **첨부 전체 화면 뷰어** — 좌우로 넘겨 본다(← → 키, ESC 로 닫기).
  드라이브를 새 창으로 여는 것보다 현장에서 빠르다. 원본으로 나가는 길도 남겼다
- **홈에 이번 달 처리 현황** — 앱을 켜면 상태별 건수가 보인다.
  아직 안 끝난 것(진행 중·교체 예정)을 왼쪽에 둔다. 사본이 없으면 구역을 숨긴다

### 수정 (사고 예방)
- **동시 [업데이트] 로 가이드가 말없이 덮이던 문제** — 기기가 보내던 `baseRevision` 을
  서버가 무시하고 있었다. 두 기사가 각자 오프라인에서 고치고 둘 다 올리면 먼저 올린
  쪽이 **경고 없이 사라졌다.** 이제 뒤처진 기기는 **409 로 거절**하고, 앱이 누가·언제
  올렸는지 보여 준 뒤 사용자가 확인해야만 덮어쓴다.
  `baseRevision` 을 안 보내는 예전 앱은 그대로 통과시킨다(배포 중 호환)
- **재고 [−] 연타 시 한 번만 반영되던 문제** — 두 호출이 같은 현재 수량을 읽었다.
  2개를 썼는데 1개만 빠졌다. 재고 변경을 한 번에 하나씩 처리하도록 줄을 세웠다
- **드라이브 용량이 차면 사진이 조용히 안 올라가던 문제** — 업로드 전에 남은 공간을
  재 보고, 모자라면 이유를 분명히 남긴다. 설정의 [연결 테스트] 에도 남은 용량이 뜨고
  500MB 아래면 눈에 띄게 알린다
- **`BH_DATA_DIR` 로 데이터 폴더를 옮길 수 있게 했다** — 이게 없어서 테스트가
  **운영 데이터를 직접 건드렸다.** v3.0 때 리포트가 날아갔고 이번 작업 중에도
  충돌 테스트가 운영 가이드를 덮어써 백업에서 되살렸다. 두 번 같은 사고가 났으므로
  구조로 막았다. `tests/` 와 CI 는 항상 이 값을 쓴다

### 추가 (운영)
- **데이터 자동 백업 (Windows)** — `tools\백업 자동실행 등록.bat` 더블클릭이면
  매일 저녁 7시에 백업한다. 해제용 파일도 함께. 도구는 있었지만 자동 실행이 안 걸려 있었다
- **가이드도 시트에서 고칠 수 있다 (양방향)** — 가이드 탭에 숨김 ID 열이 생겼고,
  [⬆ 업데이트] 때 시트에서 고친 내용을 받아온다. PC 에서 여러 건을 정리하는 편이
  태블릿보다 빠르다.
  안전 규칙 둘: **내 변경이 아직 안 올라갔으면 받지 않는다**(현장에서 쓴 것이 사라지지
  않도록), **시트에서 줄을 지운 것은 삭제로 보지 않는다**(실수로 지웠을 때 되돌릴 수 없으므로)
- **Apps Script 자동 배포 워크플로** — `.gs` 를 고쳐 올리면 스크립트 갱신 + 기존 배포를
  새 버전으로(URL 유지). 인증(`CLASP_CREDENTIALS`·`APPS_SCRIPT_ID`)이 등록되기 전에는
  실패가 아니라 **건너뛴다**
- **외부 접속 안내** — `tools\외부접속 열기.bat`(임시 주소, 계정 불필요)과
  `docs/외부접속.md`(고정 주소·보안 주의). 가이드 공유만 아직 사무실 서버를 거치기 때문

### 수정 (그 밖에)
- 편집 중 이스케이프가 깨져 `syncnow.js` 와 `google-apps-script.gs` 의 문자열이
  줄 중간에서 끊겨 있던 것을 바로잡았다. 같은 사고를 찾는 검사기를 scratchpad 에 두었다

### 검증
- 테스트 **7종** 전부 통과 (`test_publish_conflict.py` 신설 — 덮어쓰기 방지를
  실제 서버 2기기 시나리오로 고정, CI 에도 추가)
- 브라우저 실동작: [이어서 작성] → 식당명·시리얼·코드 자동 채움 + 지난 방문 2건 표시,
  첨부 뷰어 1/2 → 2/2 넘김, 홈 처리 현황 타일, 이력 7건·타일 3/1/2/1
- 백업 작업 스케줄러 실제 실행 확인 (Last Result 0, 로그 기록)

## [3.2.0] - 2026-09-01
> **디자인 적용 · [업데이트] 재정의 · PIN 제거 · 대청소**

### 변경 (호환성 깨짐)
- **[⬆ 업데이트]의 뜻이 바뀌었다.** 예전에는 "내 화면을 사무실 서버의 공개본으로 만들기"
  였지만, 팀 공유의 중심이 구글 시트로 옮겨가면서 그 뜻이 사라졌다. 이제 누르면
  **오프라인에서 쌓인 것을 시트로 올리고, 이어서 시트의 최신 내용을 받아온다.**
  현장에서 일하다 인터넷이 되는 곳에서 한 번만 누르면 된다.
  상단 칩이 `시트와 같은 내용` / `올릴 내용 N건` / `오프라인 · 올릴 것 N건` 을 계속 보여 준다
- **접근 PIN 기능을 전부 없앴다.** `web/js/auth.js`, `/api/auth*`, `--reset-pin`,
  설정 화면의 PIN 구역, 관련 테스트까지 제거
- **디자인 전면 교체** — 비욘드허니컴 브랜드(흰색·검정·코발트 `#0047AB`), Pretendard.
  앱 셸을 `.topbar + .offline-banner + .screen + .tabbar` 로 재구성,
  홈은 **검색 우선** 배치, 이력은 상태 타일 + 조밀한 표.
  색·간격·라운드는 전부 `app.css` 상단 토큰에서만 나온다 (새 hex/px 없음)
- Pretendard 를 `web/fonts/` 에 **자체 호스팅** — 현장은 오프라인이라 CDN 을 쓸 수 없다.
  서비스워커 캐시 목록에도 넣었다

### 수정 (실제 버그)
- **재고 수량이 서버 값과 어긋나던 문제** — `sync.js` 가 수량 키를 공백으로 이어 붙여
  `store.js` 의 키(`NUL` 구분)와 달랐다. 서버가 돌려준 값이 통째로 버려지고
  읽을 수 없는 줄만 쌓였다. 키를 만드는 곳을 `store.qtyKey` 하나로 모았다
- **오프라인에서 추가한 품목이 사라지던 문제** — 시트를 받아올 때 기기에만 있는 줄을
  지워, 전송되기도 전에 없어졌다. 올릴 구조 변경이 대기 중이면 지키고 지나가도록 했다
- **한 종류가 실패하면 대기분 전체가 막히던 문제** — 가이드 탭 갱신 한 번이 실패하면
  밀린 리포트가 전부 안 올라갔다. 종류별로 따로 감쌌고, 리포트 업로드는 무엇이 실패했든 시도한다
- **작성 중인 리포트의 사진이 지워지던 문제** — 미참조 사진 정리가 임시보관(localStorage)의
  첨부를 보지 못했다
- **이력 상태 변경이 실패해도 기기에 남던 문제** — 시트에 못 쓴 값을 되돌리지 않아
  다음에 열면 거짓을 보여 줬다
- **오프라인에서 화면 코드 갱신이 앱을 못 열게 하던 문제** — 캐시를 지운 뒤 받아오지
  못하면 복구할 수 없었다. 연결이 확실할 때만 지운다
- **큰 업로드·잘못된 메서드 뒤 연결이 깨지던 문제** — 본문을 읽지 않고 응답해
  남은 바이트가 다음 요청에 섞였다. 그 연결을 닫도록 했다
- 홈에서 뒤로가기 버튼이 보이던 문제 (`[hidden]` 이 `.btn` 의 display 에 덮였다)

### 제거 (죽은 코드)
- `app/db.py` — 가이드·재고·항목 CRUD 전부 (v3.0 부터 기기에서 처리, `sync.py` 는 자체 SQL).
  **965줄 → 515줄**
- `app/api.py` — `/api/meta`, `GET /api/settings`, `/api/settings/sheets-test`, `_int`, `q`
- `app/sheets.py` — `test_connection` (기기가 구글로 직접 확인한다)
- `web/js/publish.js` → `syncnow.js` 로 대체, 미사용 export 4종 제거
- `web/js/local/idb.js` 의 NUL(0x00) 제거 — `store.js` 에 이어 세 번째

### 검증
- 테스트 6종 전부 통과 (없앤 경로가 되살아나지 않는지 검사 추가)
- 브라우저 실동작: 7개 화면 렌더링, 라이트/다크, [업데이트] 왕복
  (대기 1건 → 시트 기록 확인 → 대기 0)

## [3.1.0] - 2026-08-25
> **이력 화면 개편** — 시트를 읽어 와 검색·월별·상태 관리까지 앱에서 한다. (C안)

### 추가
- **이력 화면이 구글 시트를 읽어 온다**: 월별 탭(`2026-08` 등)을 그대로 목록으로 표시.
  이제 이력이 내 기기 것만이 아니라 **팀 전체가 올린 기록**이다
- **상태별 건수 타일 4칸**: 화면을 열면 조치 완료·모니터링·진행 중·교체 예정 건수가 먼저 보이고,
  누르면 그 상태만 걸러진다 — "이번 달 안 끝난 게 몇 건인지" 세지 않아도 된다
- **검색**: 식당명과 날짜를 한 칸에서 함께 찾는다
- **월 선택**: 시트의 월별 탭을 드롭다운으로 전환
- **줄마다 상태 바꾸기**: 4종 중 고르면 시트의 `상태` 열에 즉시 기록.
  오프라인이면 대기열에 쌓았다가 연결될 때 자동 반영
- **첨부를 구글 드라이브에 저장**: 스프레드시트 옆 `현장 리포트 첨부` 폴더에 넣고
  시트에는 링크만 남긴다. **영상도 올라가고**, 이력 상세에서 미리보기로 볼 수 있다
  (한 개 20MB, 리포트당 25MB)

### 변경
- **`상태` 열을 새로 만든다** — 작성 화면의 `처리 결과`와 **분리**했다.
  처리 결과는 그날 현장에서 확정된 사실이라 그대로 남고, 상태는 이후 바뀌는 추적 표시다.
  첫 상태만 처리 결과에서 가져온다 (완료→조치 완료, 재방문 필요→조치 진행 중,
  부품 대기→교체 예정, 모니터링→모니터링)
- 사진을 시트 칸에 그림으로 삽입하던 방식을 걷어냈다 — 앱이 되읽을 수 없고 영상이 안 됐다.
  (예전 앱이 보낸 요청은 그대로 받아 준다)
- 이력 목록은 줄 번호가 아니라 **작성일시로 정렬** — 시트에서 사람이 줄을 옮겨도 안전

### 수정
- `web/js/local/store.js` 에 섞여 있던 NUL(0x00) 문자 제거 — git 이 파일을
  바이너리로 취급해 diff 를 볼 수 없던 문제 (`.gs` 에 이어 두 번째)

### 검증
- 테스트 7종으로 확장 (`test_report_history.py` 추가 — Apps Script 와 앱이 같은 계약을
  쓰는지, 소스에 NUL 이 없는지 고정)
- 모의 Apps Script 서버로 브라우저 실동작 확인: 상태 타일 필터, 식당명·날짜 검색,
  월 전환, 상태 변경이 시트에 기록됨, 오프라인 폴백, 오프라인 변경 → 재연결 시 자동 반영

## [3.0.0] - 2026-08-17
> **오프라인 앱** — 자료를 기기로 옮겨 인터넷 없이 전부 동작하게 하고, APK 로 설치한다. (D-6=A)

### 추가
- **오프라인 전면 지원**: 가이드 열람·검색·추가·수정·삭제, 리포트 작성(사진 포함),
  재고 수정까지 **인터넷 없이 전부** 동작. 자료를 기기 안 IndexedDB 에 보관
- **대기열(outbox)**: 오프라인에서 누른 시트 업로드·재고 변경을 쌓아 두었다가
  연결되면 **자동 처리** (누를 버튼 없음)
- **APK 안드로이드 앱**: `android/` WebView 껍데기 + GitHub Actions 자동 빌드 →
  Releases 에서 태블릿으로 다운로드·설치 (작업 PC 에 개발 도구 불필요)
- **시트 직접 전송**: 기기 → Apps Script 로 바로 보내 **사무실 밖 LTE 에서도 업로드**
  (브라우저가 막으면 서버 우회 통로로 자동 전환)
- 서비스워커: 앱 화면을 기기에 담아 오프라인에서도 앱이 열림 + 사진을 기기 저장본으로 제공
- 연결 상태 띠(📴 오프라인 / ⏳ 대기 N건), 재고 '반영 대기' 표시, 리포트 `업로드 대기` 상태
- 팀 공통 설정(시트 주소)을 기기로 함께 내려받아 오프라인에서도 전송 가능
- 오프라인 PIN 잠금 해제 (마지막 성공한 PIN 확인값을 기기에 보관)

### 변경
- **서버 역할 축소**: 팀 공개본 보관 + 동기화만 담당.
  기기별 작업본(`data/drafts/`) 구조 제거 — **기기 자체가 작업본**
- 가이드·재고·리포트 CRUD API 제거 (전부 기기에서 처리)
- 리포트는 서버에 저장하지 않음 (기기 → 구글 시트)
- APK 앱을 위해 그 출처에 한해 교차 출처 허용 + `X-Access-Token` 헤더 인증
- 테스트 재구성 (6종): 동기화 모델 · 시트 전송 · APK 연결 규칙 · 오프라인 실행 보장 등

### 제거
- 최근 본 가이드 20건 임시 보관(v2.1 D-4) — 전체 자료가 기기에 있어 역할 종료

### 수정
- 서비스워커 캐시 목록에 `net.js` 누락 → 오프라인에서 빈 화면 (테스트로 고정)
- 사전 확인(preflight) 응답에 허용 출처 헤더가 두 번 나가 브라우저가 거부하던 문제
- 재고 '대기 중' 표시가 전송 완료 후에도 남던 문제 → 대기열을 유일한 근거로 변경
- 재고 수량 변경 후 상단 요약(총 개수)이 갱신되지 않던 문제

### 자료 이전
- v2 에서 서버에 남아 있던 리포트를 앱 첫 실행 시 기기로 자동 이전 (`/api/sync/legacy`)
- 개발 중 실수로 서버 `report` 표를 지웠다가 백업에서 복구 (WORKLOG 참고).
  이후 마이그레이션이 예전 리포트를 지우지 않도록 수정

## [2.1.0] - 2026-08-04
### 추가
- **접근 보호**: 팀 공용 PIN(선택). 기기당 1회 입력, 사진까지 보호, `--reset-pin` 복구
- **재고 수량 즉시 공유**: 수량은 [업데이트] 없이 바로 반영 (품목·차량 추가/삭제는 기존대로)
- **오프라인 완충**: 최근 본 가이드 20건 기기 보관 → 연결 끊겨도 열람·검색
- **하단 탭바**: 홈·재고·새 리포트·이력·설정 (라벨 상시 표시), 항목 설정은 설정 안으로
- 첫 실행 시 [업데이트] 개념 1회 안내
- `tools/backup.py`(자동 백업) · `tools/cleanup_media.py`(미참조 사진 정리)
- GitHub Actions CI — push 마다 테스트 6종 자동 실행
- 설정 화면 아코디언
### 수정
- **동시 [업데이트] 잠금 추가** — 두 사람이 같이 눌러도 데이터가 꼬이지 않음
- **API 응답 `Cache-Control: no-store`** — 브라우저가 오래된 데이터를 재사용하던 문제
- 사진 EXIF 회전 반영 (구형 태블릿에서 눕는 문제)
- CORS `*` 헤더 제거

## [2.0.1] - 2026-08-04
### 수정
- 리포트 사진을 구글 시트에 **이미지로 직접 삽입** (기존: 열리지 않는 링크)
- 업로드 전 사진 자동 축소 (최대 1600px / JPEG, 약 87% 용량 감소)
- 하드코딩 도메인 제거 → 접속한 서버 주소 자동 감지

## [2.0.0] - 2026-08-03
### 변경 (호환성 깨짐)
- 노션 연동 **전부 제거** → 구글 스프레드시트(Apps Script 웹 앱) 업로드로 교체
- 월별 시트 자동 생성: 1행 공백 / 2행 항목명 / 3행부터 데이터
- 실시간 공유 제거 → **공개본/작업본 모델**: [업데이트] 버튼을 눌러야 전체 적용,
  타인 업데이트는 자동 수신 (가져오기 버튼 삭제)
- 오프라인 기능(서비스 워커·대기열·HTTPS 설치 흐름) 전부 제거 — 온라인 전용
- "필드" → "항목 설정", "내보내기" → "업데이트" 명칭 변경
### 추가
- 기기별 작업본(`data/drafts/`), 기기 이름 1회 등록, 충돌 경고 + [내 변경 버리고 최신 받기]
- 이전 버전 DB 자동 마이그레이션

## [1.2.0] - 2026-08-02
### 추가
- 팀 공유: 내보내기/가져오기 + 리비전 + diff 미리보기 + 노란 버튼 알림
- 오프라인: 서비스 워커 캐시, 오프라인 검색, 미전송 대기열, `--https` 모드
- 리포트 기기 공유(카카오톡 등), 앱 설치 버튼

## [1.1.0] - 2026-08-01
### 추가
- 차량 추가/삭제 관리 (vehicle 테이블 분리)
- 노션 DB 스키마 임포트로 리포트 항목 자동 생성 (v2.0 에서 제거)
- PWA(manifest·아이콘), 빌드 해시 기반 화면 코드 자동 갱신
### 수정
- macOS python.org 빌드의 SSL 루트 인증서 부재 대응 (CA 번들 자동 탐색)
- 토큰 칸 URL 입력 등 사용자 실수 검증

## [1.0.0] - 2026-08-01
### 최초 릴리스
- 오류코드/HW SOP/SW 명령어 가이드 CRUD + 통합 검색 + 단계 체크리스트
- 스타리아 차량 재고 `[-]/[+]` 관리
- 동적 리포트 폼 + 자동 임시보관 + 사진 촬영/첨부
- Python 표준 라이브러리 단일 서버 (외부 의존성 0)
